# .secretscanignore — paths excluded from GitHub secret scanning alerts.
# Each line is a path or path glob. Add entries here for paths that
# legitimately contain placeholder credentials (e.g. .env.example).
# See https://docs.github.com/en/code-security/secret-scanning for details.

.env.example
tests/fixtures/
